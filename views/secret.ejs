<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Restricted Access</title>
    <link rel="icon" href="/logo.png">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body { 
            font-family: 'Poppins', sans-serif; 
            overflow-x: hidden; 
            background-color: #020617; /* Very dark blue/black */
        }
        .login-container {
            background: rgba(15, 23, 42, 0.6);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(56, 189, 248, 0.1); /* Light blue border hint */
            box-shadow: 0 0 50px rgba(56, 189, 248, 0.05);
            animation: fadeIn 0.8s ease-out;
        }
        input {
            background-color: rgba(2, 6, 23, 0.8) !important;
            border: 1px solid #1e293b !important;
            color: #e2e8f0 !important; 
            letter-spacing: 0.05em;
        }
        input:focus { 
            border-color: #38bdf8 !important; /* Sky blue focus */
            box-shadow: 0 0 0 2px rgba(56, 189, 248, 0.1) !important; 
            outline: none !important; 
        }
        .glow-text {
            text-shadow: 0 0 20px rgba(56, 189, 248, 0.5);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: scale(0.95); }
            to { opacity: 1; transform: scale(1); }
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-4 relative">
    
    <div class="absolute inset-0 overflow-hidden pointer-events-none">
        <div class="absolute top-[-10%] left-[-10%] w-1/2 h-1/2 bg-blue-900/10 rounded-full blur-[120px]"></div>
        <div class="absolute bottom-[-10%] right-[-10%] w-1/2 h-1/2 bg-cyan-900/10 rounded-full blur-[120px]"></div>
    </div>

    <main class="w-full max-w-md z-10">
        <div class="login-container p-8 md:p-10 rounded-2xl">
            
            <div class="text-center mb-8">
                <div class="inline-flex items-center justify-center w-16 h-16 rounded-full bg-slate-900 border border-slate-800 mb-4 shadow-lg">
                    <svg class="w-8 h-8 text-sky-500" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 15v2m-6 4h12a2 2 0 002-2v-6a2 2 0 00-2-2H6a2 2 0 00-2 2v6a2 2 0 002 2zm10-10V7a4 4 0 00-8 0v4h8z"></path></svg>
                </div>
                <h1 class="text-2xl font-bold text-white tracking-widest uppercase glow-text">Top Secret</h1>
                <p class="text-slate-500 text-xs mt-2 tracking-wider">CLASSIFIED ACCESS LEVEL 5</p>
            </div>

            <form action="/secret-login" method="POST">
                <div class="mb-5">
                    <label class="block text-xs font-semibold text-sky-500 uppercase tracking-wider mb-2 ml-1">Agent ID</label>
                    <input type="text" name="username" class="w-full p-3 rounded-lg transition-all" placeholder="Identity" required autocomplete="off">
                </div>
                
                <div class="mb-8">
                    <label class="block text-xs font-semibold text-sky-500 uppercase tracking-wider mb-2 ml-1">Passcode</label>
                    <input type="password" name="password" class="w-full p-3 rounded-lg transition-all" placeholder="••••••••" required>
                </div>

                <% if (typeof error !== 'undefined' && error) { %>
                    <div class="mb-6 p-3 bg-red-500/5 border border-red-500/20 rounded text-red-400 text-xs text-center tracking-wide">
                        ⚠️ <%= error %>
                    </div>
                <% } %>
                
                <button type="submit" class="w-full bg-sky-600 hover:bg-sky-500 text-white font-bold py-3 px-4 rounded-lg transition-all duration-300 shadow-[0_0_20px_rgba(14,165,233,0.3)] hover:shadow-[0_0_30px_rgba(14,165,233,0.5)] uppercase tracking-widest text-sm">
                    Authorize
                </button>
            </form>
            
            <div class="text-center mt-8 border-t border-slate-800 pt-6">
                <a href="/" class="text-xs text-slate-600 hover:text-slate-400 transition-colors uppercase tracking-wide">
                    Abort Mission (Return Home)
                </a>
            </div>
        </div>
    </main>

</body>
</html>