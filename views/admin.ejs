<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Admin Login</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Style yang sama dari homepage Anda untuk konsistensi */
        body { 
            background-color: #1a1a2e; 
            min-height: 100vh; 
            font-family: sans-serif; 
            overflow-x: hidden; 
        }
        /* Menggunakan style yang sama dengan .welcome-container */
        .login-container {
            background-color: #16213e; 
            box-shadow: 0 10px 25px rgba(0,0,0,0.3);
            animation: fadeIn 0.6s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="relative">

    <main class="relative flex items-center justify-center p-4 min-h-screen">
    
        <div class="login-container text-white p-10 md:p-12 rounded-2xl max-w-md w-full mx-4">
            
            <svg class="w-16 h-16 mx-auto mb-6 text-blue-400" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0zM4.501 20.118a7.5 7.5 0 0114.998 0A1.875 1.875 0 0118.375 22.5H5.625a1.875 1.875 0 01-1.125-2.382z"></path>
            </svg>
            
            <h1 class="text-3xl font-extrabold mb-6 text-center">Admin Login</h1>

            <form action="/admin" method="POST">
                <div class="mb-5">
                    <label for="username" class="block text-sm font-medium text-gray-300 mb-2">Username</label>
                    <input type="text" id="username" name="username" 
                           class="w-full p-3 rounded-lg bg-gray-800/50 border border-gray-700 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500" 
                           placeholder="Masukkan username" required>
                </div>
                
                <div class="mb-6">
                    <label for="password" class="block text-sm font-medium text-gray-300 mb-2">Password</label>
                    <input type="password" id="password" name="password" 
                           class="w-full p-3 rounded-lg bg-gray-800/50 border border-gray-700 text-white placeholder-gray-500 focus:outline-none focus:ring-2 focus:ring-blue-500" 
                           placeholder="Masukkan password" required>
                </div>

                <p id="message" class="message text-center text-red-400 mb-4 text-sm">
                     <% if (typeof error !== 'undefined' && error) { %>
                         <%= error %>
                     <% } %>
                </p>
                
                <button type="submit" 
                        class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold p-3 rounded-lg transition-colors duration-200">
                    Masuk
                </button>
            </form>
            
            <div class="text-center mt-6">
                <a href="/" class="text-sm text-blue-400 hover:text-blue-300 transition-colors">
                    &larr; Kembali ke Home
                </a>
            </div>
        </div>

    </main>
    
</body>
</html>