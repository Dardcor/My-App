<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no">
    <title>Dardcor</title>
    <link rel="icon" href="/logo.png">
    <script src="https://cdn.tailwindcss.com"></script>
    
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    
    <style>
        /* [DIUBAH] Menggunakan font Poppins */
        body { 
            font-family: 'Poppins', sans-serif; 
            overflow-x: hidden; 
        }
        
        /* [DIUBAH] Desain 'login-container' menjadi Frosted Glass */
        .login-container {
            /* Warna solid #16213e diubah menjadi transparan */
            background-color: rgba(22, 33, 62, 0.6); /* #16213e dengan opasitas 60% */
            
            /* Efek Kaca Frost */
            backdrop-filter: blur(16px) saturate(120%);
            -webkit-backdrop-filter: blur(16px) saturate(120%);
            
            /* Border tipis untuk menonjolkan efek kaca */
            border: 1px solid rgba(255, 255, 255, 0.1);
            
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            animation: fadeIn 0.6s ease-out;
        }

        /* Input field dibuat sedikit lebih transparan */
        input {
            background-color: rgba(15, 23, 42, 0.75) !important; /* #0f172a dengan opasitas 75% */
            border: 1px solid #334155 !important; 
            color: white !important; 
        }
        input:focus { 
            border-color: #3b82f6 !important; 
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2) !important; 
            outline: none !important; 
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
    </style>
</head>
<body class="relative">

    <div class="relative min-h-screen flex items-center justify-center p-4">
        
        <img src="/header.jpg" alt="Header" class="absolute inset-0 w-full h-full object-cover z-0">
        
        <div class="absolute inset-0 bg-black/60 z-10"></div>

        <main class="relative z-20 w-full max-w-md">
            <div class="login-container text-white p-10 md:p-12 rounded-2xl">
                
                <svg class="w-16 h-16 mx-auto mb-6 text-blue-400" fill="none" stroke="currentColor" stroke-width="1.5" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 6a3.75 3.75 0 11-7.5 0 3.75 3.75 0 017.5 0zM4.501 20.118a7.5 7.5 0 0114.998 0A1.875 1.875 0 0118.375 22.5H5.625a1.875 1.875 0 01-1.125-2.382z"></path>
                </svg>
                
                <h1 class="text-3xl font-extrabold mb-6 text-center">Admin</h1>

                <form action="/admin" method="POST">
                    <div class="mb-5">
                        <label for="username" class="block text-sm font-medium text-gray-300 mb-2">Username</label>
                        <input type="text" id="username" name="username" 
                               class="w-full p-3 rounded-lg placeholder-gray-500" 
                               placeholder="Masukkan username" required>
                    </div>
                    
                    <div class="mb-6">
                        <label for="password" class="block text-sm font-medium text-gray-300 mb-2">Password</label>
                        <input type="password" id="password" name="password" 
                               class="w-full p-3 rounded-lg placeholder-gray-500" 
                               placeholder="Masukkan password" required>
                    </div>

                    <p id="message" class="message text-center text-red-400 mb-4 text-sm">
                         <% if (typeof error !== 'undefined' && error) { %>
                             <%= error %>
                         <% } %>
                    </p>
                    
                    <button type="submit" 
                            class="w-full bg-blue-600 hover:bg-blue-700 text-white font-bold p-3 rounded-lg transition-colors duration-200">
                        Login
                    </button>
                </form>
                
                <div class="text-center mt-6">
                    <a href="/" class="text-sm text-blue-400 hover:text-blue-300 transition-colors">
                        &larr; Kembali ke Home
                    </a>
                </div>
            </div>
        </main>
    </div>
    
</body>
</html>